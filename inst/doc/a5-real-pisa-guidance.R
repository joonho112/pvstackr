## ----setup, include = FALSE---------------------------------------------------
# pvstackr ships no real PISA data (the bundled pisa_tiny data are synthetic),
# so the chunks of this article are not run (eval = FALSE below) and show only
# the form of each call. The session-info chunk sets eval = TRUE and runs.
knitr::opts_chunk$set(
  collapse  = TRUE,
  comment   = "#>",
  fig.align = "center",
  out.width = "85%",
  eval      = FALSE
)

## ----library------------------------------------------------------------------
# library(pvstackr)

## ----session-info, eval = TRUE------------------------------------------------
sessionInfo()

